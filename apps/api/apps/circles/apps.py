from django.apps import AppConfig


class CirclesConfig(AppConfig):
    name = "apps.circles"
    label = "circles"
    verbose_name = "Круги — группы поддержки"
    default_auto_field = "django.db.models.BigAutoField"
