from django.apps import AppConfig


class CallsConfig(AppConfig):
    name = "apps.calls"
    label = "calls"
    verbose_name = "Звонки: оценки и жалобы на связь"
    default_auto_field = "django.db.models.BigAutoField"
